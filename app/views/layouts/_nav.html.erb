<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Online Reseller</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to 'Home', products_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
         <%= link_to 'New product', new_product_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to 'My Purchased products', user_products_path, class: "nav-link" %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Profile Options
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %>
          <div class="dropdown-divider"></div>
          <%= link_to 'My products', my_products_path, class: "dropdown-item" %>
        </div>
      </li>
    </ul>

    <ul class="navbar-nav">
    <% presenter = UserPresenter.new(current_user) %> 
     <li class="nav-item">
        <p class="welcome">Welcome <%= presenter.full_name if user_signed_in? %>!
      </li>
    </ul>

    <ul class="navbar-nav">
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= button_to "Sign out", destroy_user_session_path, class: "btn btn-outline-danger sign-out-btn", :method => :delete  %>
        <% end %>
       </li>
    </ul>
  
    <%= form_tag products_path, method: :get, class: "form-inline my-2 my-lg-0" do %>
      <%= text_field_tag :query, "", class: "form-control mr-sm-2", placeholder: "Search for products"  %>
      <%= submit_tag "Search", class: "btn btn-outline-success my-2 my-sm-0" %>
    <% end %>
  </div>
</nav>
